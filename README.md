# library-management-system
The Python code creates an Object-Oriented Library Management System that uses Book objects to manage the catalog. It features book issue/return logic with automated fine calculation. Data is permanently stored via file handling, and the system uses exception handling for stability and input validation.
